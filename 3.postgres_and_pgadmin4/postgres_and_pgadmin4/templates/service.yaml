apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgres_and_pgadmin4.fullname" . }}
  labels:
    {{- include "postgres_and_pgadmin4.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "postgres_and_pgadmin4.selectorLabels" . | nindent 4 }}
