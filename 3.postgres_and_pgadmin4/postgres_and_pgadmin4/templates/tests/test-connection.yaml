apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgres_and_pgadmin4.fullname" . }}-test-connection"
  labels:
    {{- include "postgres_and_pgadmin4.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postgres_and_pgadmin4.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
